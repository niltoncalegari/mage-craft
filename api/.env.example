PORT=4000
MONGO_URI=mongodb://localhost:27017/mage-craft
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=7d
# Shared secret servers (e.g. the Go match server) use to POST /api/matches
# without a per-user JWT. See docs/accounts-ranking-dashboard.md §4.
MATCH_INGEST_API_KEY=change-me-in-production
CORS_ORIGIN=http://localhost:5173
